# V2 backlog (not in V1 scope)

Items called out in phase plans or product objectives that are **out of scope** for the current V1 delivery. Promote here to “next” only when the family explicitly priorities them.

| Area | Idea | Notes |
|------|------|--------|
| Schools UI | **Card / gallery view** alongside the table | Teachable alternative layout; same data source |
| Recruiting | **Gmail connector** | Populate `CoachContact` from inbox (objectives §10 V2) |
| Applications | **Deadline automation** / calendar sync | V1 has deadline fields only |
| Data | **Automated seed regeneration** script | One-command refresh from external canonical list |
| Product | **Law-program-specific workflows** | Product is not a pre-law tracker (objectives §12) |
| Imports | **Import contract versioning** | Explicit schema/version in Cowork JSON envelopes |

When adding a V2 feature, update [university_advisor_data_spec.md](context/university_advisor_data_spec.md) and import validation in `lib/validation/` in the same change.
